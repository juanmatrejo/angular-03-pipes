import { UnCapitalPipe } from './un-capital.pipe';

describe('UnCapitalPipe', () => {
  it('create an instance', () => {
    const pipe = new UnCapitalPipe();
    expect(pipe).toBeTruthy();
  });
});
